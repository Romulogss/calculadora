import React from 'react';
import { Text, View } from 'react-native';

export default props => (
    <View style={{backgroundColor:'#2196F3',alignItems:'center',padding:12}}>
        <Text style={{color:'#fff',fontSize:20,fontWeight:'bold'}}>Calculadora</Text>
    </View>
);

